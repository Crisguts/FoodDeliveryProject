package org.example;

public class Coffee extends Item implements Drink{
    /**
     * consume() will be used to decrement from the stock inside the database
     */
    @Override
    public void consume() {

    }
}
